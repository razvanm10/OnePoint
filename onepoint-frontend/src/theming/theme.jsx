import {createTheme} from "@mui/material";

const theme = createTheme({

    palette: {
        primary: {
            main: "#00838f",
            input: "#e0f7fa",
            card: "#00806b"
        },
        secondary: {
            main: "#b2ebf2"
        }
    },

});

export default theme;